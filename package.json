{
  "name": "yield-park",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": ["contracts", "agent", "web"],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "format": "pnpm -r format",
    "lint": "pnpm -r lint"
  }
}
